{% extends '@Layout/Page/Admin/base.html.twig' %}

{% block body %}
    <div>
        <p>{{ image.name }}</p>
        <p>{{ image.description }}</p>
        <img src="{{ vich_uploader_asset(image, 'file') }}">
    </div>
{% endblock body %}